                	<div class="item">
                        <div class="product">
                            <div class="product_img">
                                <a href="">
                                    <img src="/assets/uploads/products/{{product.picture1}}" alt="{{product.name}}">
                                </a>
                                <div class="product_action_box">
                                    <ul class="list_none pr_action_btn">
                                        <li class="add-to-cart"><a href="{{path('cart_add' , {'id':product.id})}}"><i class="icon-basket-loaded"></i> Ajouter au panier</a></li>
                                        <li><a href="{{ path('product_details', {'slug' : product.slug })}}" class=""><i class="icon-magnifier-add"></i></a></li>
                                    </ul>
                                </div>
                            </div>
                            <div class="product_info">
                                <h6 class="product_title"><a href="">{{product.name}}</a></h6>
                                <div class="product_price">
                                    <span class="price">{{product.price/100}} €</span>
                                    <del>${{product.price*1.35/100}}</del>
                                </div>
                                <div class="pr_desc">
                                    <p>Description : </p>
                                    <p>{{product.description}}</p>
                                </div>
                            </div>
                        </div>
                    </div>